import 'package:flutter/material.dart';

class FontPage extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(title: Text("폰트"),),
      body: Center(
        child: Text("폰트"),
      ),
    );
  }
}